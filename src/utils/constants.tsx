import { AvailabilityFilters } from "../interfaces/ExerciseInterface";

export const initAvailabilityFilters: AvailabilityFilters = {
  id: 1,
  name: "curl bicep",
  variantes: ["parado", "sentado"],
};
